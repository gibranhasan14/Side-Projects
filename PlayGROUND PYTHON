SQL Queries in DB-API: Python Code


import sqlite3

# Fetch records from either chinook.db
db = sqlite3.connect("chinook.db")
c = db.cursor()
QUERY = "SELECT BillingCity, max(TOTAL) as MAXAMT FROM Invoice;"
c.execute(QUERY)
rows = c.fetchall()

print "Row data:"
print rows

print "your output:"
for row in rows:
  print "  ", row[0:]


import pandas as pd
df = pd.DataFrame(rows)
print df
























import sqlite3

Fetch records from either chinook.db
db = sqlite3.connect("chinook.db")
c = db.cursor()
QUERY = "SELECT * FROM Invoice;"
c.execute(QUERY)
rows = c.fetchall()

import sqlite3

db = sqlite3.connect("chinook.db")
c = db.cursor()
QUERY = "SELECT * FROM Invoice;"
c.execute(QUERY)
rows = c.fetchall()
